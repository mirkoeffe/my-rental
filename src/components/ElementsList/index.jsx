/* import "./styles.css";

function ElementsList({ elements = [] }) {
  return (
    <div className="element-list">
      {elements.map((element, index) => (
        <div key={index} className="element-item">
          <h3>{element.name}</h3>
          <h4>{element.city}</h4>
          <img
            src={element.picture_url.url}
            alt={element.name}
            style={{ width: "100%", height: "auto" }}
          />
        </div>
      ))}
    </div>
  );
}

export default ElementsList;
 */
